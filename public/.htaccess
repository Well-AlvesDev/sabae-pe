Options -MultiViews
RewriteEngine On

# Let Apache serve files generated by Angular directly.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Send Angular routes to the application shell.
RewriteRule ^ index.html [L]